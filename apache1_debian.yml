---
- name: Install and configure Apache on Debian systems
  hosts: webservers
  become: true
  tasks:
    - name: Install Apache
      ansible.builtin.apt:
        name: apache2
        state: present

    - name: Enable & start Apache
      ansible.builtin.service:
        name: apache2
        state: started
        enabled: true
